node_modules/
dist/
.turbo/
*.log
.DS_Store
*.tsbuildinfo

# Playwright artifacts — generated by `pnpm test:native` / playwright test runs.
test-results/
playwright-report/

# Un-ignore packages/ — root .gitignore has **/packages/* for NuGet restore,
# but web/packages/ contains first-party source that must be committed.
!packages/
!packages/**
