{
  "_comment": "Gate smoke for --record (#510): opens a self-contained fixture .alo (no game install, same fixtures the render-goldens lane uses), spawns one burst, and records ~15 frames. No cursor track (cursor-free heartbeat-ack path). The record-smoke gate lane asserts the output directory has >0 PNG frames — the coverage that was missing when #510 (0-frame records) crept in.",
  "open": "web/apps/editor/tests/fixtures/nt-5-singleton.alo",
  "fps": 60,
  "width": 1280,
  "height": 960,
  "durationMs": 250,
  "out": "tests/.tmp/record-smoke-frames",
  "openSettleMs": 300,
  "tracks": [
    { "at": 0,  "kind": "spawner/start",   "params": { "mode": "manual", "enabled": true, "burstSize": 1, "position": [0, 0, 0], "maxLifetimeSec": 40.0 } },
    { "at": 30, "kind": "spawner/trigger", "params": {} }
  ]
}
