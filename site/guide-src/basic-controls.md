# Basic Editor Controls

**Status:** Planned — not yet written.

This page will cover the fundamental viewport interactions that aren't obvious from the UI alone —
the "here's how you do things" basics a new user has no way to discover on their own.

Notes to fold in, from a Discord conversation with **IRampagingRhin0**:

- Holding Shift previews a particle in the viewport; clicking places it. The poster didn't know this until being told directly — call it out explicitly rather than assuming it's discoverable.
